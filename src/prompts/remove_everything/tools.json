[
  {
    "type": "function",
    "function": {
      "name": "exec",
      "description": "在当前正在运行的 Linux 机器上执行 bash 命令。",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "需要执行的命令。"
          }
        },
        "required": ["command"]
      }
    }
  }
]
